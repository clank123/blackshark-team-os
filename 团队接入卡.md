# 团队首次接入卡

前提：电脑上的 Codex 可以正常使用。仓库公开可读，不需要 GitHub 账号。

## 第一次使用

1. 在 Codex 中发送：“请把 `https://github.com/clank123/blackshark-team-os.git` 下载到我的文档目录，完成后告诉我文件夹位置。”
2. 在 Codex 中选择“打开文件夹”，选中仓库根目录 `blackshark-team-os`。
3. 新建任务并发送：“请读取 README.md 和 AGENTS.md，告诉我当前可以处理哪些门店运营任务。”
4. 验证任务：“为二店生成 2026 年 8 月运营方案预览，只生成预览，不发布飞书。”
5. 能识别二店门店卡、七段式方案和单表推进结构，即完成接入。

## 以后使用

每次开始新的月度任务前，在原来的 Codex 工作区发送：“请先同步当前公开仓库到最新版本，不要改动 `03_projects/`。”你的真实月度项目保存在本机 `03_projects/`，不会随仓库更新上传。

遇到仓库无法下载、无法同步或 Skill 未出现时，记录现象并联系仓库维护者，不自行改动共享文件。
