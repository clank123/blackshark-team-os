# 黑鲨团队 AI 工作区规则

本文件适用于整个仓库。开始任何任务前，先读取根目录 `README.md`，再按本文件路由上下文。

## 上下文加载

1. 读取 `01_shared_context/` 中与任务直接相关的共享上下文。
2. 读取 `02_store_cards/00-七店索引.md`。
3. 只加载目标门店的一张详细门店卡，不同时加载七张门店卡。
4. 读取本月项目 `00-input/` 中的会议材料和明确补充。

真值优先级：本月已确认会议事实 > 门店卡当前状态 > 门店卡长期基因 > 通用经营框架。

会议最新事实可以修正旧的“当前状态”，但不能静默改写“长期基因”。长期基因发生变化时，把证据写入 `05_reviews/月度复盘候选/`，等待知识库负责人确认。

## 写入边界

- 不修改 `01_shared_context/`。
- 不修改 `02_store_cards/`。
- 不修改 `04_templates/`。
- 不修改 `.agents/skills/`。
- 本月任务只写入 `03_projects/YYYY-MM-门店名称/`。
- 待回收经验只写入 `05_reviews/月度复盘候选/`。
- 不保存登录凭证、访问令牌、员工私人材料或未授权经营数据。

## 月度方案边界

- 不重新发明主方向和次方向。
- 不把未确认事项写成已拍板。
- 缺少会议材料、主方向或三类负责人时，只输出缺口清单，不生成正式方案。
- 当前状态证据不足时标记“待门店确认”。
- 文档只使用固定七段式结构；推进表只使用一个 Base、一张业务表、十个字段和五个视图。
- 不自动增加仪表盘、自动催办、多表控制台或额外视图。

## 外部发布

- 发布前必须展示本次文档和推进表预览，并获得操作者明确确认。
- 只在操作者指定的黑鲨知识库位置创建或更新产物。
- 创建前按完整标题搜索，避免重复创建同名文档或 Base。
- 发布后必须回读文档、Base、字段、视图和记录。
- 回读失败时，必须说明未验证，不得声称交付已经完成。
