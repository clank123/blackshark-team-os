# 黑鲨团队 AI 工作台 v0.3 规则

本文件适用于全仓。先读 README.md 与 workspace-manifest.json，按任务进入 `$blackshark-ops-router`，只加载有关方法、目标门店和当期材料。

## 两种工作

- 月度规划：从整体方向和本店现实出发，通过对话形成七段式方案与推进表。推进表可展开主项目下的子任务，飞书方案内嵌真实表视图。
- 日常求助：针对当前卡点给判断、可选做法或直接完成请求，不先要求填月度材料，不默认制作整套方案。

## 上下文与文件

事实和决定须保留来源、日期及适用范围。最新的明确决定只替代受影响的旧决定；新建议不能覆盖已确认事项。公共门店卡是历史基线，当期状态、经营数据和本地补充优先核对。

已有有效本地地图沿用。没有地图但完整工作台已在当前目录时，仍可读取材料和对话；用户要求本地生成时使用 manifest 声明的个人目录，告知所用路径，无需先配置整个知识库。移动、迁移、覆盖同名内容或写到未指定的外部目录时，才按已有冲突规则处理。当前任务无需文件时不创建地图或个人档案。

## 方法与模板

已确认方向直接继承；尚未清楚时，AI 根据已有材料给候选判断并解释取舍，再问影响选择的少量问题。责任人、预算或日期缺失只影响相关任务，不阻断整份工作稿。正式下发的可执行任务应具备承接人、时间、交付及验收；未定事项单列，不能伪装成已拍板。

方案沿用七段式模板。推进表沿用一张业务表、六状态、五个默认视图，新增任务编号和父任务以承接稳定关系；现有表的其他字段保留，升级前检查映射。具体定义以当前模板版本为准。固定的是结构与含义，不是每月动作数量、金额、客群或手段。不要强制所有板块都做。

## 归属与持续更新

官方基线包括方法、门店历史卡、模板和 Skill；用户要求修订时可维护，更新团队副本时保留本地修改。个人资料、当期会议、门店当前记录与经营产物只留在个人工作区，不上传公共仓库。门店新事实可在本次授权范围内更新对应本地当前记录；方法改进作为有来源的候选，不能把一次效果直接升格成全店规则。

只接回当前任务实际收到、能读取且与该店有关的材料，不承诺监控其他任务、聊天或全部门店。

## 外部执行

先完成可审阅结果。请求本地草稿不授权外部写入；用户已明确要求将具体版本写到具体目标时继承该授权，不重复确认。需新增资源承诺、目标不明确、覆盖冲突或扩大权限时，只处理对应缺口。

文档、Base 数据、父子展开和文档内嵌四项分别回读。普通链接、自关联字段或本地树形预览均不能充当真实内嵌和父子展开已成功的证据。工具缺相应能力时保留已完成项，明确剩余步骤；不虚报全部完成。群消息和任务派送遵循用户本次明确指令。
