# 黑鲨团队 AI 工作台 v0.2 规则

本文件适用于全仓。开始任何任务前，先读取根目录 `README.md`，再按本文件加载最小必要上下文。

## 启动与上下文加载

1. 先读取 `workspace-manifest.json`。
2. 优先调用 `$blackshark-ops-router`，由 Router 判断是否需要首次启用、匹配知识库和路由任务。
3. 如果已存在地图，按 `00_workspace/工作台地图.local.md` 定位本人的目录；个人目录结构可以修改，更新地图即可。
4. 只读取当前任务需要的共享知识和目标门店卡；不同时加载七张详细门店卡。

真值优先级：本月确认事实 > 门店卡当前状态 > 长期基因 > 通用经营框架。

本月确认事实可以修正旧的当前状态；长期基因出现变更时，先保留证据并进入回流候选，不静默覆盖原有知识。

## 三类归属与修改规则

- **官方基线**：共享上下文、门店卡、模板及其正式规则。官方基线允许本地编辑；编辑前提示后续同步风险。
- **个人扩展**：只对本人有效的配置、观察、结构和方法，可以修改并留在个人工作区。个人扩展日常编辑不需要额外确认。
- **回流候选**：可能供团队复用的经验、长期基因变更证据和改进建议进入 `05_upstream_candidates/`，由负责人决定是否吸收进官方基线。

公共版本更新时，如果官方基线存在本地差异，必须暂停自动更新，展示本地差异并确认处理方式。个人扩展日常编辑不需要额外确认；只有同步、迁移或同名覆盖时才使用防覆盖门槛。更新、迁移或同名覆盖不得静默覆盖：先展示差异和影响，再由操作者确认相应处理方式。个人目录结构可以修改，完成后把实际位置更新到工作台地图。

## 写入与数据边界

- 默认只写个人工作区；需要改官方基线或提交回流候选时，遵循上述归属规则。
- 个人配置、输入、产物和复盘不得上传公共仓库。
- 不保存凭证、员工私材、顾客信息或未授权经营数据。

## 月度方案边界

- 不重新发明主方向和次方向，不把未确认事项写成已拍板。
- 缺少会议材料、主方向或三类负责人时，只输出缺口清单，不生成正式方案。
- 当前状态证据不足时标记“待门店确认”。
- 文档只使用固定七段式结构；推进表只使用一个 Base、一张业务表、十个字段和五个视图。
- 不自动扩展仪表盘、工作流、多表或额外视图。

## 外部发布

- 本地产物是默认交付，不默认发布飞书。
- 只有操作者明确要求，并指定操作者本人权限和指定位置时才发布；默认目标为空。
- 发布前展示文档和推进表预览并取得确认；发布后回读文档、Base、字段、视图和记录。
- 回读失败时必须说明未验证，不得声称已完成交付。
- 不改变空间成员或权限。
